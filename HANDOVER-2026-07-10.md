# Handover Lopen te Lopen (10 juli 2026)

Dit document vat de stand van zaken samen na de grote verbeterronde van 8 juli
en de vervolgsessie daarna, plus wat er in de tussentijd in andere chats is
gebeurd. Bedoeld om in een nieuwe chat direct verder te kunnen zonder alles
opnieuw uit te zoeken. Details staan in VERBETERRONDE-2026-07-08.md en de
projectmemory; dit is de samenvatting.

## 1. Wat is afgerond sinds 8 juli

- **TypeScript**: `npx expo install` + `npx tsc --noEmit` lokaal gedraaid,
  0 fouten na twee kleine fixes (`profile?.trainingDays` in settings.tsx,
  `async` op de TaskManager-executor in backgroundLocationService.ts).
  `tsconfig.verify.json` (overblijfsel) is verwijderd.
- **Sentry**: DSN in `.env` en `eas.json` (preview+production), source maps
  ingericht (`metro.config.js` gewrapt met `withSentryConfig`,
  organization/project in de app.json-plugin), en `SENTRY_AUTH_TOKEN` staat
  als EAS-secret (bevestigd met `eas secret:list`). Zie docs/SENTRY_SETUP.md.
  Dit onderdeel is volledig klaar.
- **Website**: `website/privacy-policy.html` gesynchroniseerd met de
  bijgewerkte roottekst (Strava + Health Connect + gecorrigeerde
  Supabase-route-claim), geupload naar Strato en live geverifieerd
  (lopentelopen.nl/privacy-policy.html en /voorwaarden.html tonen 8 juli 2026,
  assets laden zonder 403).

## 2. Nog open van de oorspronkelijke actielijst

1. **Trial-offers + RevenueCat**: Play Console 14-dagen gratis proefperiode op
   het jaarlijkse base plan (alleen nieuwe abonnees), App Store Connect
   introductory offer "gratis, 2 weken" zodra dat traject zover is, en
   RevenueCat-producten/offerings herimporteren.
2. **EAS-build + fysieke toesteltest**: nieuwe build (native modules erbij),
   dan testen op toestel. Belangrijkste testpunt: achtergrond-tracking op
   iPhone met vergrendeld scherm (we gebruiken bewust geen "Altijd"-locatie-
   permissie; werkt dat niet, dan `requestBackgroundPermissionsAsync` +
   Always-description terugzetten in `backgroundLocationService.ts`). Test
   ook: crash-herstel, notificaties, trial-weergave in de paywall.
3. **Screenshots**: herwerken met tekst-overlay volgens het screenshot-plan
   in STORE_LISTING.md.

## 3. Nieuw gevonden tijdens deze handover-check (nog niet eerder op de lijst)

- **Data-safety-formulieren bijwerken**: de maandelijkse security-review van
  8 juli (reviews/2026-07-security-review.md) signaleerde dat de
  Strava-koppeling (locatie + trainingsdata delen met een derde partij, VS)
  wél in privacy-policy.html staat, maar nog niet in de Play Console
  data-safety-vragenlijst en de App Store Connect privacy-labels. Dit is een
  storereview-afwijzingsrisico, dus oppakken vóór een nieuwe indiening.
- **npm audit**: kon niet in de sandbox draaien (registry geblokkeerd).
  Draai lokaal `npm audit` (en zo nodig `npm audit fix` zonder --force) om de
  actuele kwetsbaarhedenlijst te zien. Laatste bekende stand (29 juni): 0
  kwetsbaarheden.
- **Git: grote hoeveelheid ongecommitte wijzigingen.** Sinds de laatste
  commit (`01524914`, "eas.json uit versiebeheer") is er niets gecommit,
  terwijl vrijwel de hele grote verbeterronde (premium/trial, background GPS,
  retentie, Sentry, alle documentatie) nu als werkboom-wijziging in de
  projectmap staat, plus een aantal nieuwe ongetrackte bestanden
  (VERBETERRONDE-2026-07-08.md, docs/SENTRY_SETUP.md,
  reviews/2026-07-security-review.md, en de nieuwe services
  backgroundLocationService.ts, crashReporting.ts, notificationService.ts,
  reviewService.ts, runRecoveryService.ts). **Aanbeveling: commit dit werk
  (eventueel in een paar logische stukken) voordat er nog meer bovenop komt.**

## 4. Wat in parallelle chats is gebeurd (geen actie nu nodig, ter info)

- **iOS/App Store-traject**: apart traject, 6 fases. Keuze: Apple Developer
  account als Individual, Apple ID larsvdb1@hotmail.com. Codekant van fase 3
  (platform-afhankelijke RevenueCat-key, iOS pakt straks automatisch de
  Apple-key) staat al klaar in purchaseService.ts/.env.example/eas.json. Wacht
  op Lars om de Apple Developer-inschrijving af te ronden; dat is niet gebeurd
  in deze sessie, dus check zelf de status als je hiermee verder wil. Zie
  ook [[project-ios-appstore-traject]] in het geheugen.
- **Marketing**: de wekelijkse influencer-scout voegde op 9 juli 3 nieuwe
  leads toe aan marketing/influencers.md (Loopgids Maarten, 024 Run Club,
  podcast LoopPraat). Niet urgent.
- **Let op verwarring**: er loopt ook een sessie/systeem rond een
  "Strato site deployment", maar die gaat over de DroneVision Studios-website
  (info@dronevisionstudios.nl), niet over lopentelopen.nl. Niet met elkaar
  verwarren.
- Er is een apart "DroneVision HQ"-agentsysteem opgezet (dvs-ideeenmaker,
  dvs-data-analist, dvs-producer, dvs-publisher) als kopie van het patroon
  van het Lopen te Lopen HQ-dashboard, voor een ander project. Raakt dit
  project niet inhoudelijk.

## 5. Losse documenten die verouderd zijn

`docs/STAPPENPLAN_LARS.md` dateert van 1 juli en is achterhaald door
VERBETERRONDE-2026-07-08.md en dit document; niet meer als leidraad gebruiken.

## Kortom: eerstvolgende acties

1. Commit het openstaande werk in git (zie punt 3).
2. Play Console + App Store Connect: trial-offers instellen EN de
   data-safety/privacy-labels bijwerken voor Strava.
3. RevenueCat-producten herimporteren.
4. Nieuwe EAS-build + fysieke toesteltest (iPhone-achtergrondtracking is het
   spannendste testpunt).
5. Screenshots herwerken.
6. Optioneel: npm audit lokaal, iOS-traject vervolgen.
